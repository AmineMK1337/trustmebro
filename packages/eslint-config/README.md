# `@turbo/eslint-config`

Collection of internal eslint configurations.

## Architecture Alignment

This package is shared tooling and is not part of runtime verification flow changes (including the adaptive source-verification architecture).
